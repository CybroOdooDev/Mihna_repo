<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Inherit hr.attendance list view to show regularization field -->
    <record id="view_attendance_tree_inherit" model="ir.ui.view">
        <field name="name">hr.attendance.list.inherit.attendance.regularization</field>
        <field name="model">hr.attendance</field>
        <field name="inherit_id" ref="hr_attendance.view_attendance_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='check_out']" position="after">
                <field name="regularization" optional="show"/>
            </xpath>
        </field>
    </record>

    <!-- Inherit hr.attendance form view to show regularization field -->
    <record id="hr_attendance_view_form_inherit" model="ir.ui.view">
        <field name="name">hr.attendance.form.inherit.attendance.regularization</field>
        <field name="model">hr.attendance</field>
        <field name="inherit_id" ref="hr_attendance.hr_attendance_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='check_out']" position="after">
                <field name="regularization" readonly="1"/>
            </xpath>
        </field>
    </record>
</odoo>
