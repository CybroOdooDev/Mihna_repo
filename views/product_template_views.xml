<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="product_template_view_form_inherit_subscription" model="ir.ui.view">
        <field name="name">product.template.form.inherit.subscription</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_form_view"/>
        <field name="arch" type="xml">
            <!-- Add Subscription Product checkbox -->
            <xpath expr="//div[@name='options']" position="inside">
                <span class="d-inline-block">
                    <field name="recurring_ok"/>
                    <label for="recurring_ok"/>
                </span>
            </xpath>
            <!-- Add Subscription Plan dropdown in General Info tab -->
            <xpath expr="//page[@name='general_information']/group" position="inside">
                <group string="Subscription Settings" invisible="not recurring_ok">
                    <field name="subscription_plan_id" required="recurring_ok"/>
                </group>
            </xpath>
        </field>
    </record>
</odoo>
